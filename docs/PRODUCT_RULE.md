# Scope 1 product rule (locked)

**Chosen: Option A � full gate**

- Applicants may work on drafts and run live calculations in the wizard.
- **Official** dashboard access and branded report download are only available after admin sets `scope1-assessments.reviewStatus` to **approved** and parent `assessments.status` to **APPROVED**.
- OTP is required before `/scope1/report` downloads (same pattern as Scope 2 certificate).
- No �official� certificate URL is emailed before approval.

**Not in scope (Option B):** pre-submit official PDF treated as final deliverable.

This matches the unified flow: book ? assess ? submit ? admin review ? approve ? email ? OTP ? download.
